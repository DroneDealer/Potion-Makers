using UnityEngine;

public class PotionMix : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Committing another hour of Unity because the extensions are not working. 
        // just to clarify: in the last 2 hour update, i was making a bakground, adding sprites, and making animations.
        // In this 1 hour update, I was animating more sprites, adding more assets, and finishing thier hitboxes


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
